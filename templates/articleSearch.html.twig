{% extends ('base.html.twig') %}


    {% block main %}

        <div class="container-list">
            <div class="title-list">
                <h1>Il y a {{ articles | length}} d'articles correspondant à votre recherche :</h1>

            </div>

            <div class="list">
                {% for article in articles %}
                    <article>
                        <a href={{ path('articleShow', {'id': article.id} ) }}>

                            <h3>{{ article.title }}</h3>
                            <p>{{ article.content }}</p>

                        </a>
                    </article>
                {% endfor %}
            </div>
        </div>

    {% endblock main %}