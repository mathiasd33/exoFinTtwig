{% extends 'base.html.twig' %}
{% block main %}
    <body>
    <div class="show">
        <h1>{{ article.title }}</h1>
        <div>{{ article.content }}</div>
        <p>{{ article.createdAt | date ('y-m-d') }}</p>

    </div>

    </body>
{% endblock main %}