{% extends 'admin/base.html.twig' %}

{% block main %}


    {#    {% form_theme tagForm 'bootstrap_4_layout.html.twig' %}#}
    {#    {{ form(tagForm) }}#}

    {#    #}{# équivalent de la balise d'ouverture du form #}
    {{ form_start(tagForm) }}

    {#        #}{# permet d'afficher une row du formulaire (label + input) #}
    {#        {{ form_row(tagForm.title) }}#}
    {#        <div class="form_row">#}
    {#            {{ form_row(tagForm.content) }}#}
    {#            {{ form_row(tagForm.createdAt) }}#}
    {#        </div>#}
    {#        {{ form_row(tagForm.isPublished) }}#}
    {#        {{ form_row(tagForm.submit) }}#}


    <section class="border border-grey w-50 mt-5 p-3 m-auto mb-5 bg bg-dark text-light rounded">

        <h1 class="text-center">Ajouter une tag</h1>

        <div class="m-4 ">
            {{ form_errors(tagForm.title) }}
            {#permet de rendre le label du champs title #}
            {{ form_label(tagForm.title , 'TITRE',{'label_attr': {'class': 'form-label'}}) }}
            {#permet de rendre l'input du champs title #}
            {{ form_widget(tagForm.title , {'attr': {'class': 'form-control', 'placeholder': 'remplissez le titre...'}}) }}
        </div>

        <div class="m-4">
            {{ form_label(tagForm.color , 'COULEUR',{'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(tagForm.color , {'attr': {'class': 'form-control'}}) }}
        </div>

    <div class="d-flex justify-content-center">
        {{ form_widget(tagForm.submit , {'attr': {'class': 'btn btn-primary w-25 '}}) }}
    </div>

    {{ form_end(tagForm) }}

{% endblock main %}