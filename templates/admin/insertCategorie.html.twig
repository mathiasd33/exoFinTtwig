{% extends 'admin/base.html.twig' %}

{% block main %}


    {#    {% form_theme categorieForm 'bootstrap_4_layout.html.twig' %}#}
    {#    {{ form(categorieForm) }}#}

    {#    #}{# équivalent de la balise d'ouverture du form #}
    {{ form_start(categorieForm) }}

    {#        #}{# permet d'afficher une row du formulaire (label + input) #}
    {#        {{ form_row(categorieForm.title) }}#}
    {#        <div class="form_row">#}
    {#            {{ form_row(categorieForm.content) }}#}
    {#            {{ form_row(categorieForm.createdAt) }}#}
    {#        </div>#}
    {#        {{ form_row(categorieForm.isPublished) }}#}
    {#        {{ form_row(categorieForm.submit) }}#}


<section class="border border-grey w-50 mt-5 p-3 m-auto mb-5 bg bg-dark text-light rounded">

    <h1 class="text-center">Ajouter une catégorie</h1>

    <div class="m-4 ">
        {{ form_errors(categorieForm.title) }}
        {#permet de rendre le label du champs title #}
        {{ form_label(categorieForm.title , 'TITRE',{'label_attr': {'class': 'form-label'}}) }}
        {#permet de rendre l'input du champs title #}
        {{ form_widget(categorieForm.title , {'attr': {'class': 'form-control', 'placeholder': 'remplissez le titre...'}}) }}
    </div>

    <div class="m-4">
        {{ form_errors(categorieForm.description) }}
        {{ form_label(categorieForm.description, 'CONTENU',{'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(categorieForm.description, {'attr': {'class': 'form-control', 'placeholder': 'remplissez le contenu...'}}) }}
    </div>

    <div class="d-flex justify-content-center">
        {{ form_widget(categorieForm.submit , {'attr': {'class': 'btn btn-primary w-25 '}}) }}
    </div>


    {# équivalent de la balise de fermeture du form #}
    {{ form_end(categorieForm) }}
</section>

{% endblock main %}
